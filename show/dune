(library
  (name clangml_show)
  (public_name clangml.show)
  (preprocess (staged_pps override ppx_show))
  (libraries clangml ppx_show.runtime)
; -warning 30: Constructor defined in several types
  (flags -w -30))