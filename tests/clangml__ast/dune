(executable
  (name clangml__ast)
  (preprocess (pps pattern.ppx))
  (link_flags -linkall)
  (libraries compiler-libs clangml clangml.lift pattern.runtime))

(rule
  (targets clangml__ast.ml)
  (deps ../../clangml/clang__ast.ml)
  (action (run ocamlcodoc %{deps} -o %{targets})))