(library
  (name clangml_lift)
  (public_name clangml.lift)
  (preprocess (staged_pps override ppx_deriving.std ppxlib.metaquot ppxlib.traverse))
  (libraries ppxlib.metaquot ppxlib.metaquot_lifters clangml))