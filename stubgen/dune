(executable
  (name stubgen)
  (flags -open Stdcompat)
  (link_flags -cclib (:include "../config/ldflags_quoted.sexp") -cclib (:include "../config/libs_quoted.sexp"))
  (libraries unix stdcompat compiler-libs.common pcre clangml cmdliner))